JUICE / GAME FEEL v2 — deeper polish (render layer only; sim deterministic + untouched). Apply the Vlambeer/Nijman "Art of Screenshake" principles.
Add / improve (all render-side, reading sim state + s.fxEvents frame-to-frame; performance.now/Math.random allowed in src/render):
1) HIT-STOP / freeze-frames: on a big impact (boss/miniboss hit, big kill, player damage), briefly freeze/slow the visual for ~40-80ms (render-only: hold the frame / skip advancing render interpolation; DO NOT alter the sim step) for punchy feedback.
2) FLOATING DAMAGE NUMBERS: on enemy hp drops, spawn small rising, fading damage-number text (pooled, capped ~40); bigger/critical hits = bigger/brighter. Reads hp deltas frame-to-frame (no sim change).
3) ENEMY KNOCKBACK (visual): nudge an enemy sprite back briefly on hit (render offset, decaying) — do not move sim positions.
4) MUZZLE FLASH + projectile trails: a brief flash at the player when a weapon fires (weaponAoeFires / projectile appears); short fading trails behind projectiles.
5) Sharper SCREENSHAKE curves (trauma^2, directional toward impact) + a subtle zoom-punch on boss spawn/defeat.
6) LEVEL-UP / EVOLUTION flair: a brief slow-mo + bright burst + rays on level-up and on weapon evolve.
7) PICKUP streaks: XP orbs leave a short streak when magnetized; a small pop + count-up on the XP bar.
8) Keep all existing fx (vignette, death-pops, hit-flash, glow, telegraph) — layer on top. Cap particle/text pools for 60fps mobile.
CONSTRAINTS: render only; NO src/sim edits; no test/accept/package.json edits; gate green (sim unchanged). Verify visually.
ACCEPTANCE: hits show damage numbers + knockback + hit-stop; firing shows muzzle flash/trails; level-up/evolve/boss get flair; framerate stable; portrait/camera intact.