GOAL: Enforce the GDD magenta danger convention. Magenta = EXACTLY #ff0099 (GDD danger color). Two rules: (a) ALL enemy projectiles that can damage the player render in magenta #ff0099; (b) all telegraphed special-attack zones render as a magenta marker that appears with enough lead time BEFORE the hit, and a telegraph sound (sfx.telegraph) plays when a telegraph first appears. Timing stays deterministic in the sim; only COLOR and the sound live in render.

FILE-PATH NOTE: the boss-attack sim file is actually at src/sim/systems/boss-attack.ts (there is NO src/sim/boss-attack.ts). Edit src/sim/systems/boss-attack.ts. The render file is src/render/stage.ts.

HARD RULES: NEVER edit game/test/** or game/accept/**. src/sim/systems/boss-attack.ts is DETERMINISTIC — NO Math.random / Date.now / performance.now / requestAnimationFrame; use s.rng only, and only if you must draw randomness (you should NOT need any new rng here). src/render/stage.ts MAY use performance.now + navigator, must not be imported by sim. Keep the gate green.

AVAILABLE ASSET KEYS: fx.telegraph (64x64 magenta sprite). Audio: sfx.telegraph. Use getTexture('fx.telegraph') in render and audioManager.playSound('sfx.telegraph') in render. These keys may not be in the manifests yet — getTexture auto-declares a placeholder and never throws; audioManager.playSound(key) is a no-op if the audio asset is not real yet (safe to call).

CURRENT STATE TO GROUND ON:
- src/sim/systems/boss-attack.ts already: fires boss enemy projectiles into s.enemyProjectiles on a deterministic timer; pushes mini-boss telegraphs (s.telegraphs) every MINI_BOSS_ATTACK.interval; pushes a boss telegraph every 6s when in a stage. A Telegraph = { id, x, y, radius, time, duration, damage, damageDealt }. runTick (src/sim/loop.ts) advances t.time each tick and applies damage when t.time>=t.duration. So telegraphs ALREADY appear `duration` seconds before the hit — that IS the lead time. Do NOT shorten it.
- src/render/stage.ts already draws enemy projectiles and telegraphs, BUT it uses 0xff00ff (pure magenta/fuchsia), which is NOT the GDD danger color #ff0099. It draws telegraphs as Graphics circles (warning outline + growing fill, then a blast). There is currently NO fx.telegraph sprite and NO sfx.telegraph playback.

IMPLEMENTATION:
A) COLOR CORRECTION (render/stage.ts): Replace every telegraph/enemy-projectile magenta 0xff00ff with the GDD danger color 0xff0099. Specifically in sync(): the enemyProjGfx.fill(0xff00ff) becomes fill(0xff0099); every telegraphGfx stroke/fill that uses 0xff00ff becomes 0xff0099. Define a const DANGER = 0xff0099 at module scope and use it everywhere a danger-magenta is drawn so the convention is centralized. (Keep the white blast outline as-is; that is not the danger color.) This satisfies: enemy projectiles ALWAYS magenta #ff0099, telegraph zones magenta #ff0099.
B) TELEGRAPH SPRITE (render/stage.ts): For each active telegraph in its WARNING phase (t.time < t.duration), in addition to (or instead of) the Graphics circle, place a fx.telegraph sprite centered at (t.x,t.y) scaled so its footprint roughly matches t.radius*2 (fx.telegraph is 64x64; set sprite width=height=t.radius*2). Keep a small pool/Map of telegraph sprites keyed by telegraph id (mirror the enemy-sprite reuse pattern) OR simplest: keep ONE reusable Container of sprites and hide unused ones each frame. Re-pull getTexture('fx.telegraph') each frame and reassign texture only if changed (placeholder→real async swap). Tint the sprite to DANGER if the placeholder is not already magenta (sprite.tint = 0xff0099). The Graphics outline may remain for the growing-fill readability; the sprite adds the recognizable magenta marker. Keep the blast phase (t.time>=t.duration) drawing as the bright filled circle in DANGER.
C) TELEGRAPH SOUND (render/stage.ts): Play sfx.telegraph exactly once when a telegraph FIRST appears. Detect newly-appeared telegraphs in render by tracking the set of telegraph ids seen last frame: keep a private Set<number> of previously-seen telegraph ids; each sync(), for any active telegraph whose id is NOT in the set, call audioManager.playSound('sfx.telegraph') and add it; rebuild/prune the set to only currently-active ids so a re-used id can re-trigger later. Import audioManager from ./audio (already used elsewhere in render). Do NOT play the sound in the sim.
D) SIM SIDE (src/sim/systems/boss-attack.ts): NO color changes (sim has no color). Confirm/keep the deterministic telegraph timing and the invariant comment that enemy projectiles are drawn magenta by render. If the file's header comment says 0xff00ff or 'magenta', update the comment to reference the GDD color #ff0099 for accuracy. Ensure telegraph `duration` gives the player enough time to move away (mini-boss duration is 1.5s, boss 1.8s — leave as-is; these are the lead times). Do not add rng.

VERIFICATION (visual + gate): (1) Run the sim past a boss/mini-boss spawn (tests use short bossSpawnSec overrides; for preview use a short override or the Ashen geyser event which pushes telegraphs every 8s) and screenshot: a telegraph zone shows as magenta #ff0099 (sprite + outline) BEFORE the blast, and enemy projectiles render magenta #ff0099. (2) Confirm sfx.telegraph is called when a telegraph first appears (check via a console spy or that audioManager.playSound('sfx.telegraph') is reached — reached-by-playback grep must find the literal call in stage.ts). (3) Deterministic gate/tests still pass (no sim behavior changed, only render color + a comment).